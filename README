## 引言

lwIP 是一个小型独立的 TCP/IP 协议栈实现。

lwIP TCP/IP 实现的重点是减少 RAM 使用量，同时仍然具备完整的 TCP 功能。这使得 lwIP 适合在具有数十千字节可用 RAM 和大约 40 千字节代码 ROM 的嵌入式系统中使用。

lwIP 最初由 Adam Dunkels 在瑞典计算机科学研究所 (SICS) 的计算机与网络架构实验室开发，现在由全球开发者网络进行开发和维护。

## 特性

- IP（互联网协议，IPv4 和 IPv6），包括通过多个网络接口进行数据包转发
- ICMP（互联网控制消息协议）用于网络维护和调试
- IGMP（互联网组管理协议）用于多播流量管理
- MLD（IPv6 多播监听器发现）。旨在符合 RFC 2710。不支持 MLDv2
- ND（IPv6 邻居发现和无状态地址自动配置）。旨在符合 RFC 4861（邻居发现）和 RFC 4862（地址自动配置）
- DHCP、AutoIP/APIPA（零配置）和（无状态）DHCPv6
- UDP（用户数据报协议），包括实验性的 UDP-lite 扩展
- TCP（传输控制协议），具有拥塞控制、RTT 估计、快速恢复/快速重传以及发送 SACK
- 原始/本地 API 以增强性能
- 可选的 Berkeley 风格套接字 API
- TLS：可选的分层 TCP（“altcp”），为任何基于 TCP 的协议提供几乎透明的 TLS（已移植到 mbedTLS）（有关更多信息，请参见更新日志）
- PPPoS 和 PPPoE（串行/以太网点对点协议）
- DNS（域名解析器，包括 mDNS）
- 6LoWPAN（通过 IEEE 802.15.4、BLE 或 ZEP）

## 应用

- 带有 SSI 和 CGI 的 HTTP 服务器（通过 altcp 支持 HTTPS）
- 带有 MIB 编译器的 SNMPv2c 代理（简单网络管理协议），通过 altcp 支持 v3
- SNTP（简单网络时间协议）
- NetBIOS 名称服务响应器
- MDNS（多播 DNS）响应器
- iPerf 服务器实现
- MQTT 客户端（通过 altcp 支持 TLS）

## 许可证

lwIP 在 BSD 许可证下免费提供。

## 开发

lwIP 已发展成为嵌入式设备的优秀 TCP/IP 栈，使用该栈的开发者通常会提交错误修复、改进和新功能，以进一步提高其实用性。

lwIP 的开发托管在 Savannah，这是一个软件开发、维护和分发的中心点。任何人都可以通过使用 Savannah 的界面、Git 和邮件列表来帮助改善 lwIP。一个核心开发团队将向 Git 源代码树提交更改。

lwIP TCP/IP 栈在 'lwip' Git 模块中维护，贡献（如平台移植）在 'contrib' Git 模块中。

有关用户和开发者的 Git 服务器访问详细信息，请参阅 doc/savannah.txt。

当前的 Git 树可以通过网页浏览：
- [lwIP Git](http://git.savannah.gnu.org/cgit/lwip.git)
- [lwIP contrib Git](http://git.savannah.gnu.org/cgit/lwip/lwip-contrib.git)

通过 lwIP 项目页面提交补丁和错误：
- [lwIP 项目页面](http://savannah.nongnu.org/projects/lwip/)

持续集成构建（GCC、clang）：
- [Travis CI](https://travis-ci.org/yarrick/lwip-merged)

## 文档

源代码的自我文档定期从当前 Git 源中提取，并可从此网页获取：
- [lwIP 文档](http://www.nongnu.org/lwip/)

现在有一个不断增长的关于 lwIP 的维基：
- [lwIP 维基](http://lwip.wikia.com/wiki/LwIP_Wiki)

此外，还有可以订阅的邮件列表：
- [lwIP 邮件列表](http://savannah.nongnu.org/mail/?group=lwip)
以及可搜索的归档：
- [lwIP 用户邮件归档](http://lists.nongnu.org/archive/html/lwip-users/)
- [lwIP 开发邮件归档](http://lists.nongnu.org/archive/html/lwip-devel/)

lwIP 最初由 Adam Dunkels 编写：
- [Adam Dunkels](http://dunkels.com/adam/)

阅读 Adam 的论文、docs/ 中的文件、浏览源代码文档以及浏览邮件列表归档是熟悉 lwIP 设计的好方法。

Adam Dunkels <adam@sics.se>  
Leon Woestenberg <leon.woestenberg@gmx.net>
